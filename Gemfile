# Only needed to preview the site locally (`bundle exec jekyll serve`).
# GitHub Pages builds the live site itself with the same gem set.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
